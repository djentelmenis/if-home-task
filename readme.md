## To run

First install dependencies:

```sh
npm i
```

To run locally on `localhost:3000`:

```sh
npm start
```

To create a production build:

```sh
npm run build
```

To run tests:

```sh
npm run test
```
